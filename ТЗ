ТЗ
1. Возможность хранения и отображения информации:
  - категория об-я (отдельно таблица категорий, строгая форма);
  - производитель (отдельно таблица производителей, строгая форма);
  - количество;
  - единицы измерений (отдельно таблица производителей, строгая форма)
2. Регистрация событий:
  - создание записи
  - удаление
  - изменение (вход/выход оборудования, бронирование
3. Возможность бронирования оборудования:
  - кто забронировал;
  - куда (какая установка, таблица установок, строгая форма);
